# 애니 체크 작업 규칙

## 기능을 바꿨으면 문서도 같이 바꾼다

기능을 **추가·수정·삭제**할 때마다 아래 네 곳을 같은 커밋에서 점검한다.
해당 없으면 넘어가도 되지만, **확인은 반드시 한다.**

| 문서 | 파일 | 갱신 기준 |
|---|---|---|
| 업데이트 내용 | `lib/src/data/patch_notes.dart` | 사용자가 체감하는 변화가 있으면 맨 위에 새 버전 항목 추가 |
| 설명서 | `lib/src/features/settings/help_screen.dart` | 화면이나 동작이 생기거나 바뀌면 항목 추가·수정, 없어지면 삭제 |
| 개인정보처리방침 | `lib/src/features/legal/legal_screen.dart` (`privacyDocument`) | 수집·전송·보관하는 정보가 달라지면 문구와 시행일 갱신 |
| 데이터 출처 및 저작권 | `lib/src/features/legal/legal_screen.dart` (`dataSourceDocument`) | 외부 데이터 사용 방식, 표시 방식, 프록시 동작이 달라지면 갱신 |

### 특히 놓치기 쉬운 것

- **문서에 적힌 내용이 사실과 어긋나는지** 확인한다.
  예: "뉴스 기사 이미지는 표시하지 않습니다"라고 적어두고 이미지 표시 기능을 켠 적이 있다.
- 서버로 **새로 보내는 값**이 생기면 개인정보처리방침에 적는다.
  예: 피드백에 앱 버전을 함께 보내기 시작함, 스팸 방지로 IP를 하루 보관함.
- 화면을 **삭제**했으면 설명서에서도 지운다. 예: 도움말 챗봇 제거.
- 개인정보처리방침이나 데이터 출처를 고쳤으면 **시행일도 함께 갱신**한다.

## 배포

- 스토어 제출은 `node tools/play_upload.mjs <aab> production tools/release_notes_ko.txt`
- 출시 노트(`tools/release_notes_ko.txt`)와 앱 안 패치노트는 **같은 내용으로 맞춘다.**
- 관리형 게시는 꺼져 있어 심사 통과 시 자동 배포된다.

## 확인 습관

- 코드를 고쳤으면 `flutter test`와 `dart analyze`를 돌리고, 화면이 바뀌었으면 실기기에 설치해 눈으로 본다.
- 사용자 데이터(시청 기록) 구조를 건드리는 변경은 먼저 백업을 빼두고 진행한다.
